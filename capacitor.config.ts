import type { CapacitorConfig } from '@capacitor/cli'

const config: CapacitorConfig = {
  appId: 'io.github.danny270793.boxingtimer',
  appName: 'Boxing Timer',
  webDir: 'dist',
}

export default config
